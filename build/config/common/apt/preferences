# NerfOS uses runit as PID 1.
# This file is processed by live-build's lb chroot_apt phase,
# BEFORE package lists are installed — so the solver never selects systemd-sysv.

Package: systemd
Pin: release *
Pin-Priority: -1

Package: systemd-sysv
Pin: release *
Pin-Priority: -1

Package: live-config-systemd
Pin: release *
Pin-Priority: -1
